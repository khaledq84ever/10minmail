{
  "name": "10minmail",
  "version": "1.0.0",
  "description": "Disposable 10-minute temporary email web app (mail.tm backend)",
  "private": true,
  "scripts": {
    "start": "serve -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
